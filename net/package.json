{
  "name": "@bug/net",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Capa P2P de Bug: malla WebRTC (DataChannels), señalización, Lamport, replicación y token de turno.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@bug/engine": "*",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
